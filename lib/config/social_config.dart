/// Conta oficial que **novas** contas seguem ao registar-se (`followingIds` inicial).
/// O UID é obtido em runtime em `usernames/{defaultFollowUsername}`.
class SocialConfig {
  SocialConfig._();

  static const String defaultFollowUsername = 'gimie';
}
